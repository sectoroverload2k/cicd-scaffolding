# MySQL secrets for production environment
# Variables are substituted from GitHub Secrets during deployment
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secrets
  labels:
    app: mysql
    environment: prod
type: Opaque
stringData:
  username: "${MYSQL_USERNAME}"
  password: "${MYSQL_PASSWORD}"
