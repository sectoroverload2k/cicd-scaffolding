# MySQL secrets for development environment
# Variables are substituted from GitHub Secrets during deployment
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secrets
  labels:
    app: mysql
    environment: dev
type: Opaque
stringData:
  username: "${MYSQL_USERNAME}"
  password: "${MYSQL_PASSWORD}"
