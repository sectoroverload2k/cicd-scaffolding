# GitHub Container Registry (GHCR) Configuration
# Default registry for this project

# Registry URL
REGISTRY=ghcr.io

# Image naming pattern: ghcr.io/<owner>/<repo>/<service>:<tag>
# Example: ghcr.io/myorg/k8s-project-starter/api:1.0.0

# Authentication:
# GitHub Actions automatically provides GITHUB_TOKEN with packages:write permission
# For local development, use a Personal Access Token (PAT) with packages:write scope

# Login command (for local development):
# echo $GITHUB_TOKEN | docker login ghcr.io -u USERNAME --password-stdin

# Required GitHub repository settings:
# 1. Settings > Actions > General > Workflow permissions
#    - Enable "Read and write permissions"
# 2. Settings > Packages
#    - Package visibility can be public or private

# Environment variables used in workflows:
# REGISTRY - Container registry URL
# GITHUB_ACTOR - Username for authentication
# GITHUB_TOKEN - Token for authentication (automatic in Actions)
